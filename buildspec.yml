version: 0.1

phases:
  install:
    runtime-versions:
      nodejs: 10
  pre_build:
    commands:
      - echo pre-build...
  build:
    commands:
      - echo Build started
      - npm install
  post_build:
    commands:
      - echo Build completed
